<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Donations</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-image: url('don_back.jpg');
            /* Correct way to set a background image */
            background-size: cover;
            /* Ensure the background covers the entire page */
            background-position: center;
            /* Center the background image */
            background-repeat: no-repeat;
            /* Prevent the image from repeating */
        }

        .container {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            padding: 20px;
            justify-content: center;
            /* Center the flex items */
        }

        .item {
            border: 1px solid white;
            /* White border */
            border-radius: 5px;
            padding: 10px;
            width: 24%;
            /* Two items per row */
            background-color: #87ceeb;
            /* Sky blue background */
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            /* Optional shadow for better appearance */
        }

        .item:nth-child(odd) {
            background-color: #add8e6;
            /* Light blue background for odd items */
        }

        .item h2 {
            margin-top: 0;
        }

        .item button {
            padding: 5px 10px;
            background-color:#2396c8;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        .item button:hover {
            background-color:#0a555f;
        }

        /* Media query for smaller screens */
        @media (max-width: 768px) {
            .item {
                width: 100%;
                /* One item per row on smaller screens */
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <% if (messages.success) { %>
            <div class="alert alert-success">
                <%= messages.success %>
            </div>
        <% } %>

        <% donations.forEach(donation=> { %>
            <div class="item">
                <h2>
                    <%= donation.item %>
                </h2>
                <p>
                    <%= donation.description %>
                </p>
                <p>Quantity: <%= donation.quantity %>
                </p>
                <form action="/get_item/<%= donation._id %>" method="post">
                    <button type="submit">Get Item</button>
                </form>
            </div>
            <% }) %>
    </div>
</body>

</html>